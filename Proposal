<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Proposal ðŸ˜„</title>

<style>
  body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    font-family: Arial;
    background: #ffe6e6;
  }

  h1 {
    margin-bottom: 30px;
  }

  .btn-box {
    position: relative;
    width: 300px;
    height: 150px;
  }

  button {
    padding: 12px 25px;
    font-size: 18px;
    cursor: pointer;
  }

  #yes {
    background: green;
    color: white;
  }

  #no {
    position: absolute;
    background: red;
    color: white;
    transition: 0.15s;
  }
</style>
</head>

<body>

<h1>Will you be my girlfriend? â¤ï¸</h1>

<div class="btn-box">
  <button id="yes" onclick="alert('I knew it ðŸ˜â¤ï¸')">YES</button>
  <button id="no">NO</button>
</div>

<script>
  const noBtn = document.getElementById("no");
  const box = document.querySelector(".btn-box");

  function moveButton() {
    const maxX = box.clientWidth - noBtn.offsetWidth;
    const maxY = box.clientHeight - noBtn.offsetHeight;

    const x = Math.random() * maxX;
    const y = Math.random() * maxY;

    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";
  }

  // Desktop
  noBtn.addEventListener("mouseenter", moveButton);

  // Mobile support
  noBtn.addEventListener("touchstart", moveButton);
</script>

</body>
</html>
